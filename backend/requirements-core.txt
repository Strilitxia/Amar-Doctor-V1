# Main backend WITHOUT speech-to-text (faster-whisper / torch).
# Used by setup-windows.ps1. Whisper is skipped on purpose: its Bengali
# accuracy is not good enough yet, and the server degrades gracefully without
# it (server.py catches the ImportError; use the Web Speech toggle in /chat).
# The full list, including Whisper, is still in requirements.txt.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv>=1.0.0
edge-tts>=7.2.8
python-multipart==0.0.20
pydantic==2.10.4
websockets==14.2
aiofiles==24.1.0
requests==2.32.3
httpx>=0.27.0            # MuseTalk sidecar client (backend/musetalk_client.py)
